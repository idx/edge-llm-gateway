{
  "name": "edge-llm-gateway",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12",
    "@types/node": "^25.3.0",
    "typescript": "^5.7",
    "vitest": "~3.2.0",
    "wrangler": "^4.67.0"
  }
}
